name: Update XiO Device Summary

on:
  schedule:
    # runs every 6 minutes
    - cron: "*/6 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: xio-summary
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Run XiO summary script
        env:
          XIO_ACCOUNT_ID: ${{ secrets.XIO_ACCOUNT_ID }}
          XIO_SUBSCRIPTION_KEY: ${{ secrets.XIO_SUBSCRIPTION_KEY }}
        run: python xio_summary.py

      - name: Commit and push JSON if changed
        run: |
          # Check if any of the JSON files changed
          if git status --porcelain | grep -E "xio-summary.json|xio-devices-ui.json|xio-rooms-ui.json"; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"

            # Make sure we are based on the latest origin/main
            git fetch origin main
            git reset --soft origin/main

            # Add only the JSON files that actually exist
            for f in xio-summary.json xio-devices-ui.json xio-rooms-ui.json; do
              if [ -f "$f" ]; then
                git add "$f"
              fi
            done

            git commit -m "Update XiO device JSON" || exit 0
            git push origin HEAD:main
          else
            echo "No changes to XiO JSON files"
          fi
